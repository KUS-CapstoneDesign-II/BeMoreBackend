# npm configuration for BeMore Backend

# Allow passing audit to prevent CI/CD pipeline breaks for non-critical transitive dependencies
# See: docs/SECURITY.md for vulnerability assessment

# Sequelize uses validator@13.15.15 (indirect dependency)
# Known issue: validator.js URL validation bypass (GHSA-9965-vmph-33xx)
# Status: Not exploitable in our use case (validator.isURL not used in application code)
# Action: Monitor for Sequelize updates that include patched validator versions
